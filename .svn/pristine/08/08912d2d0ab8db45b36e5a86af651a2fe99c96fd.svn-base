import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SawLoaderComponent } from './saw-loader.component';

describe('SawLoaderComponent', () => {
  let component: SawLoaderComponent;
  let fixture: ComponentFixture<SawLoaderComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SawLoaderComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SawLoaderComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
